const nomes = ["amanda", "aruno", "isabela", "ana", "aurudo", "eduarda", "amapa"]
const letra = "a"

for (let i = 0; i < nomes.length; i++ ){
    if (nomes[i][0] === letra )
        console.log(nomes[i])

}